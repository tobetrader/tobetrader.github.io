{
  "name": "tobetrader",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generateData": "node scripts/build.js",
    "start": "vitepress dev ./",
    "build": "npm run generateData && vitepress build ./"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "fs-extra": "^11.1.1",
    "vitepress": "^1.0.0-beta.1"
  },
  "devDependencies": {
    "less": "^4.2.0"
  }
}
